---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: upgradeassessments.upgrade.guardian.io
spec:
  group: upgrade.guardian.io
  names:
    kind: UpgradeAssessment
    listKind: UpgradeAssessmentList
    plural: upgradeassessments
    singular: upgradeassessment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpgradeAssessment is the Schema for the upgradeassessments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UpgradeAssessmentSpec defines the desired state of UpgradeAssessment
            properties:
              checks:
                description: AssessmentChecks enables or disables individual assessment
                  checkers.
                properties:
                  admissionWebhooks:
                    type: boolean
                  capacity:
                    type: boolean
                  deprecatedApis:
                    type: boolean
                  observability:
                    type: boolean
                  pdb:
                    type: boolean
                  policyRisks:
                    type: boolean
                  readinessProbes:
                    type: boolean
                  workloadAvailability:
                    type: boolean
                type: object
              mode:
                default: ReadOnly
                description: Mode must be ReadOnly. The controller never performs
                  upgrades, drains, or workload patches.
                enum:
                - ReadOnly
                type: string
              scope:
                description: AssessmentScope defines the cluster slice to assess.
                properties:
                  namespaces:
                    description: NamespaceScope filters namespaced resources included
                      in an assessment.
                    properties:
                      exclude:
                        items:
                          type: string
                        type: array
                      include:
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              targetVersion:
                description: TargetVersion is the Kubernetes minor version being assessed,
                  for example "1.32".
                pattern: ^1\.[0-9]+$
                type: string
            required:
            - targetVersion
            type: object
          status:
            description: UpgradeAssessmentStatus defines the observed state of UpgradeAssessment
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              findings:
                items:
                  description: Finding is a risk or an assessment gap backed by observable
                    evidence.
                  properties:
                    category:
                      type: string
                    evidence:
                      items:
                        description: Evidence is an observable fact collected from
                          the cluster.
                        properties:
                          description:
                            type: string
                          id:
                            type: string
                          observed:
                            additionalProperties:
                              type: string
                            type: object
                          resource:
                            description: ResourceRef identifies a Kubernetes resource
                              used as finding evidence.
                            properties:
                              apiVersion:
                                type: string
                              kind:
                                type: string
                              name:
                                type: string
                              namespace:
                                type: string
                            type: object
                        required:
                        - description
                        - id
                        type: object
                      type: array
                    id:
                      type: string
                    message:
                      type: string
                    recommendation:
                      type: string
                    resource:
                      description: ResourceRef identifies a Kubernetes resource used
                        as finding evidence.
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                    severity:
                      description: RiskLevel is the severity of a finding or the aggregate
                        assessment risk.
                      enum:
                      - Critical
                      - High
                      - Medium
                      - Low
                      - Info
                      type: string
                    type:
                      type: string
                  required:
                  - category
                  - id
                  - message
                  - severity
                  - type
                  type: object
                type: array
              generatedPlanRef:
                description: PlanReference points an UpgradeAssessment status to the
                  generated UpgradePlan.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              phase:
                description: AssessmentPhase describes the observed lifecycle of an
                  assessment.
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              riskLevel:
                description: RiskLevel is the severity of a finding or the aggregate
                  assessment risk.
                enum:
                - Critical
                - High
                - Medium
                - Low
                - Info
                type: string
              score:
                type: integer
              summary:
                description: FindingSummary stores aggregate finding counts.
                properties:
                  critical:
                    type: integer
                  high:
                    type: integer
                  info:
                    type: integer
                  low:
                    type: integer
                  medium:
                    type: integer
                  totalFindings:
                    type: integer
                required:
                - critical
                - high
                - low
                - medium
                - totalFindings
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
